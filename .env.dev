# ── AI Agent Hub — DEV ──

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=ai_agent_hub

# Auth provider (OAuth2)
AUTH_PROVIDER_URL=http://localhost:8000
AUTH_HOST=localhost

# Frontend port
FRONTEND_PORT=80
